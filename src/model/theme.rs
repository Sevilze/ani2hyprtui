#[derive(Clone, Debug, Default)]
pub struct IndexTheme {
    pub name: String,
    pub comment: String,
    pub inherits: String,
}

#[derive(Clone, Debug, Default)]
pub struct CursorTheme {
    pub name: String,
    pub comment: String,
    pub inherits: String,
}

